fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
python-dotenv>=1.0.0
httpx>=0.27.0          # async HTTP client & TestClient for FastAPI
resend>=2.0.0          # Resend email SDK
supabase>=2.4.0        # Supabase Python client (service-role operations)
openpyxl>=3.1.0        # xlsx parsing for battery LCA import
scipy>=1.13.0          # Monte Carlo uncertainty distributions
numpy>=1.26.0          # required by scipy; also used for histogram/statistics
asteval>=0.9.31        # safe formula evaluation for Monte Carlo exchange quantities
python-multipart>=0.0.9 # multipart/form-data file uploads for FastAPI
PyJWT>=2.8.0            # Supabase JWT verification

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
